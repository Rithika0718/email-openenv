[project]
name = "email-openenv"
version = "0.1.0"
description = "Email assistant OpenEnv environment"
authors = [{ name = "Rithika" }]

dependencies = [
    "pydantic",
    "openai",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"
